[package]
name = "helix_rem"
version = "0.1.0"
edition = "2021"

# --- THE FIX STARTS HERE ---
[lib]
# This tells Rust to output 'libhelix_rem.a' for static linking
# instead of the default .rlib format
crate-type = ["staticlib"]
# ---------------------------

[dependencies]
# Dependencies go here
